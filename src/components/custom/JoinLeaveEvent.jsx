import React from "react";

export default function JoinLeaveEvent({ event, setEvent, event }) {
  return (
    <div className="max-xs:text-xs xs:text-xs sm:text-sm lg:text-base">
      JoinLeaveEvent
    </div>
  );
}
